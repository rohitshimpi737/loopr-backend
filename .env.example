# MongoDB Configuration
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/FinanceDB?retryWrites=true&w=majority&appName=backEndApi

# Alternative local MongoDB (uncomment if using local MongoDB)
# MONGODB_URI=mongodb://localhost:27017/loopr

# Server Configuration
PORT=3001

# JWT Configuration (IMPORTANT: Change this in production!)
JWT_SECRET=your-super-secure-jwt-secret-make-it-very-long-and-complex-for-production

# Environment
NODE_ENV=production

# CORS Configuration (Optional - for restricted CORS)
# ALLOWED_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com
